{"type":"convention","classification":"tactical","recorded_at":"2026-05-31T14:31:14.865Z","evidence":{"commit":"74d50ba5b6160565ab5d70b8cce9bd235442f5e7"},"content":"Rego policies default-deny: start every package with 'default allow := false' and only grant via explicit allow rules. A new endpoint or action that lacks an allow rule must be denied, not silently permitted.","id":"mx-b9bf5e"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-05-31T14:31:15.161Z","evidence":{"commit":"74d50ba5b6160565ab5d70b8cce9bd235442f5e7"},"name":"Gateway path validator must match the OpenAPI route shape exactly","description":"When a gateway Rego policy validates request paths, the validator must mirror the OpenAPI route shape exactly — same number of segments, same fixed vs variable positions. An off-by-one segment count silently rejects valid requests (403) or, worse, matches an unintended route. Add a test per path validator that asserts both a valid and an invalid path.","id":"mx-644f54"}
{"type":"failure","classification":"tactical","recorded_at":"2026-05-31T14:31:15.505Z","evidence":{"commit":"74d50ba5b6160565ab5d70b8cce9bd235442f5e7"},"description":"OPA running with '--watch' on a mounted policy directory does not always reload after the files change — especially after a branch switch that swaps the files underneath the mount. The on-disk content updates but OPA serves the previously-loaded bytes from cache, causing valid requests to 403 because the new allow rule isn't loaded.","resolution":"Restart the OPA container after editing or branch-switching policies. Verify what OPA actually loaded with: curl -s $OPA/v1/policies/<id> | jq -r .result.raw | grep <new_rule>. Treat this like any mounted-dir container cache (same class as wiremock/stale-mapping issues): restart on change.","id":"mx-4e1191"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-07-09T21:26:35.133Z","evidence":{"commit":"3c51c5be52fdebd50e7b2617ec6ac080b322eafb"},"name":"Widening-flag test discipline: pin strict explicitly, pin the invariant not the boolean","description":"When a policy gains a config-widened branch (ADR 0022 root-read exemption via data.config), existing test cells that pinned the strict behavior break under the new shipped default. The fix is NOT to delete them: re-pin them explicitly ('with data.config as {}') so the strict mode stays covered, and add flag-state cells for the new default + absent-flag + explicit-false. Crucially, re-read WHAT the old cells pinned: the 7.0.5 catalog High's cells (R5/R6) pinned list<->GET AGREEMENT, not one-sided strictness — the exemption preserves the invariant (both pass) while flipping the boolean. Also: keep 'absent config = strict' so a widening only ever activates by explicit deployment choice, and verify the residual shape empirically in BOTH flag states (opa eval --partial; a data-guarded branch folds the tag conjunct clean out of the residual when the flag is concrete-true).","id":"mx-252957"}
