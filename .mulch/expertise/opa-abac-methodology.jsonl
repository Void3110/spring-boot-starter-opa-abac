{"type":"decision","classification":"observational","recorded_at":"2026-07-06T13:28:16.205Z","evidence":{"commit":"8e7fdb55611d9c7dcc5c90af089e5f9e445b15db"},"title":"Ship the control-plane demo UI collaboratively; next slice = UserDirectory port in the library with a Keycloak adapter","rationale":"The SPA is Phase-7.0 demo scaffold (tight visual feedback -> collaborative, not autonomous). For member search, the maintainer chose a component seam over app code: a UserDirectory port in the LIBRARY modules so the Keycloak admin-API implementation is reusable by adopters (own module, optional dependency), with the example service exposing only a thin /api/v1/users/search over it.","id":"mx-12760e"}
{"type":"convention","classification":"tactical","recorded_at":"2026-07-12T22:04:57.004Z","evidence":{"commit":"c620fe3622e91d80cc8e7e606cde911c1c49130b"},"content":"Mermaid diagrams in the docs vault must render in OBSIDIAN, which is STRICTER than @mermaid-js/mermaid-cli — validating with the CLI alone is insufficient (it passed while Obsidian broke, twice). Two Obsidian gotchas: (1) SUBGRAPH TITLES must be SHORT single-line phrases — a long/parenthetical title (e.g. 'ACCUMULATORS · the loop's memory (read at ①, written at ③④)') WRAPS and clips behind the first child node; keep titles to one or two words + at most one '·' qualifier, and move the detail to an italic caption line ABOVE the ```mermaid block. (2) Nested-subgraph 'direction LR' overrides are IGNORED by Obsidian — don't rely on them; design the layout for the renderer's default vertical (TB) flow instead of fighting it. Both the methodology loop diagram and the README architecture diagram live in the repo; when editing either, render AND eyeball, and prefer short titles + TB flow. The mermaid-cli render is a syntax check, NOT a fidelity check for Obsidian.","id":"mx-250c74"}
{"type":"decision","classification":"foundational","recorded_at":"2026-07-12T21:39:07.761Z","evidence":{"commit":"6ffd7e1b761e02ea728fe6197cefb853b3e87097"},"title":"The AI-assisted workflow IS loop engineering (two nested loops + two accumulators)","rationale":"Reframing settled 2026-07-13. The plan->decompose->implement->review 'lifecycle' is not a pipeline — it is one iteration of an ENGINEERED LOOP whose defining feature is that each pass leaves artifacts that make the next pass better. TWO nested loops at two timescales: (1) INNER loop (per-slice, hours) = the per-ticket prime->build->test->*architecture-review+refactor->e2e->commit, where the * gate is self-correction (unit-green triggers review-and-refactor BEFORE heavier validation) + fix-until-green with the >=3-attempts bounded blocked-exit; (2) OUTER loop (across slices, weeks) = the retrospective-into-planning cycle. TWO ACCUMULATORS make it compound rather than evaporate: MULCH = procedural/experiential memory (ml prime before, ml record after; the autonomous-runs domain literally feeds run-outcomes back into the next slice's phase-1 planning — B4-too-big mx-76cf08 -> the slice-sizing gate is that loop having fired once); the OBSIDIAN VAULT = declarative/decisional memory (immutable ADRs, living guides, STATUS notes, review notes, QA records — the loop's state store that phase-1 reads). HONEST CAVEAT: the loop is human-gated at the outer boundary (maintainer runs /decompose, kicks off the run, does phase-4, decides merges) — a loop with a human closing it each turn, NOT a fully-autonomous self-improving flywheel (that is the Warren AI-Software-Factory direction). For a portfolio/consulting artifact that auditability is a FEATURE. CONSEQUENCE: docs/methodology/AI-ENGINEERING-METHODOLOGY.md should LEAD with the loop frame (inner+outer loops, Mulch+vault accumulators), with the 4-phase lifecycle as one iteration — stronger + more honest than the pipeline framing. Upstream credits: Mulch (Jaymin West), grill-me (Matt Pocock), Anthropic dynamic-workflows/harness patterns.","id":"mx-2d8cbb"}
{"type":"decision","classification":"tactical","recorded_at":"2026-06-11T09:30:56.660Z","evidence":{"commit":"c79e68a28e07ee7529708607a72a40344cda71e0"},"title":"Sub-agents scout and validate; the implementer implements (autonomous-run delegation policy)","rationale":"Decided with the maintainer 2026-06-11 (Phase 5.95 planning), pinned in AUTONOMOUS-IMPLEMENTATION-FLOW.md (the section-4 template line + a section-7 rationale bullet + an operator-note bullet). The per-ticket implementation stays in the main loop: across 8 shipped runs the failure modes were unpinned design semantics and rig gotchas, never context exhaustion, while cross-ticket continuity (T1 micro-decisions shaping later tickets) is what keeps a run coherent; per-ticket sub-agents re-read the whole package + re-prime Mulch each time (similar token cost) and drift at every seam. ALLOWED delegation: read-only scouting fan-outs (e.g. 'which collections assert list bodies') and log-noisy validation (run newman/build, return only the failure summary). Context pressure is handled by stopping at a checkpoint and resuming in a FRESH SESSION - the ticket status table + STATUS-0N notes are a complete handoff (the method is doc-first so the window is cache, not the source of truth).","id":"mx-309a4e"}
{"type":"decision","classification":"tactical","recorded_at":"2026-07-06T13:52:43.090Z","evidence":{"commit":"dca038cc3dca4d02c4392f35037a77e33270ff3f"},"title":"Phase 7 reordered (2026-07-06): baseline full security review NOW (7.0.5), 7.4 re-scoped to delta + publish checklist, load-test scope pinned","rationale":"Decided with the maintainer after PR #52. (1) Full security review moved BEFORE the user-directory slice: every prior deep-review was diff-scoped, so B4 tenant isolation + 6.7 control plane + PR-#52 self-service UI were never reviewed as ONE attack surface — and the control plane (team mgmt, custom-role editor, self-service create) is the classic privilege-escalation surface; findings feed the user-directory grill-me (directory search = enumeration/PII surface); that slice introduces a Keycloak ADMIN credential, so enter it from a verified baseline. (2) 7.4 pre-publish pass re-scoped to DELTA since the baseline + publish checklist (repo-history secret scan, dependency CVE audit, zero-config fail-safety, signing/supply chain) — 7.3 perf tweaks are the most security-sensitive remaining changes (decision caching = stale-authorization hazard; breaker/retry tuning changes fail-closed timing). (3) Load testing stays pre-publish (7.2), scope pinned: four library hot paths through the example services — gate overhead delta vs unguarded (p50/95/99 headline), partial-eval list path + OPA compile ceiling, enrichment multiplier (_actions fan-out/batching bound), resilience under fault (OPA slow/down, breaker cycle, B2 supplier edge) — plus the cross-service amplification ratio (1 catalog request -> N user-mgmt/OPA calls; request caches must bound it). OUT: demo UI, Keycloak login throughput, APISIX tuning. Reuse the Docker rig + B3 fault injector; tooling picked at decompose. Deliverable: PERFORMANCE.md.","id":"mx-3863b3"}
{"type":"decision","classification":"tactical","recorded_at":"2026-06-09T12:17:44.952Z","evidence":{"commit":"2f19298833a6d381e8e7749bf6e8354681a5778a"},"title":"Slice 5.5-B (hierarchy-aware list filter): the 6 settled forks + ADR 0010","rationale":"Planning grilled the list-specific forks ADR 0008 deferred; all pinned in ADR 0010 (the list fork is 0010, not 0009 — 0009 was taken by tag-requirement-subject-side). The 6 decisions: (1) ROOT-ONLY subtree roots — subtreeSpec = whole governing-root subtree iff the root-resolved role inheritably grants the verb, else empty; mid-tree per-node grants are Phase 8; reuses the existing root role lookup, no new 'which nodes' search. (2) SEAM = a new SubtreeSpecResolver (spring-data, owns root-resolution + inheritable gate + calls subtreeOf) feeding an ADDITIVE 4-arg AbacQueryService.findAuthorized(repo,scope,ctx,subtreeSpec) that owns scope.and(tagResidual.or(subtreeSpec)).and(notDenied); 3-arg PRESERVED byte-compatible (delegates subtreeSpec=null); AbacQueryService gains NO resolver dep on the composition path (receives a ready Specification). (3) notDenied = SQL mirror of the shipped leaf deny abac_deny==true -> the SQL predicate `abac_deny IS DISTINCT FROM true` over tags JSONB, AND-ed OUTSIDE the OR (folding it inside lets the subtree branch re-admit a denied row); absent tag -> not denied (matches Rego); future non-scalar deny -> route to allowlist batch (documented, not built). (4) NEW additive SPI method AncestorResolver.subtreeOf(rootType,rootId) -> Specification (NOT Set<id>, to keep the ltree pushdown + strategy behind the SPI): ltree -> path <@ <root-label> SQL pushdown (id set never materialized); CTE -> id IN (descendants) downward walk BOUNDED by maxDepth, FAIL-CLOSED to always-false on breach/cycle/error. (5) The !fullySupported allowlist BATCH path becomes hierarchy-aware: batchFilter/withResource build per-row AbacContext WITH the row's ancestor chain (4-arg Resource) so allowAll decides each row by the same Rego final_allow as the single-GET; subtreeSpec applies to the PURE-SQL path ONLY; adds an AncestorResolver dep to the batch path (constructor change = build-breaker, update all sites same commit); cheap (ltree path already loaded). (6) ONE slice, 6 tickets (T1 subtreeOf SPI+both impls+ITs; T2 SubtreeSpecResolver; T3 4-arg overload+notDenied+batch; T4 IT no-leak+re-parent-on-list; T5 starter+example adoption; T6 e2e+docs), not split — the widening + the deny narrowing are one atomic correctness unit. FAIL-CLOSED everywhere: failed subtree resolution -> narrower tag-only (never wider); subtreeSpec OR-ed INSIDE scope.and (never escapes scope); opa-abac-core NOT touched; residual/operator-set/RoleDefinition unchanged. The recurring autonomous-run pause class (a fail-open/contract semantic left unpinned) is pre-empted: root-only, notDenied-outside-OR, batch-carries-ancestors, subtreeOf-fail-closed are all pinned in advance.","id":"mx-3b45b7"}
{"type":"convention","classification":"tactical","recorded_at":"2026-06-03T08:47:12.204Z","evidence":{"commit":"d5eb4e8c1582ac63abecdbc2d06b782d8994a1a8"},"relates_to":["mx-b17da2"],"content":"Structural decisions get an ADR as part of decomposition — written UP FRONT, not after. ADRs live in docs/architecture/adr/NNNN-short-title.md (lightly-MADR: Status / Context / Decision / Considered options WITH why-rejected / Consequences) with an index README. A decision is 'structural' (=> needs an ADR) when it would be expensive to reverse or surprising to a future reader: a schema/authority shape, a module/service boundary, where a check is evaluated (app vs Rego/policy), an additive-vs-breaking choice, or a deliberate 'we did NOT do X' with real alternatives weighed. Routine choices (naming, file layout, test lib) do NOT need one. WHY up front: a feature's 00-DESIGN/01-DECOMPOSITION are LIVING docs that get rewritten and git-mv'd to implemented/ on ship, so rationale buried there drifts; an ADR is IMMUTABLE — it pins the decision + rejected alternatives at a point in time and is SUPERSEDED (never edited) by a later ADR (flip the old status to 'Superseded by [[NNNN-...]]'). Heuristic: if you catch yourself writing a 'considered & rejected' list worth keeping, that's an ADR. The planning step is now: decomposition surfaces a fork -> write/update the ADR -> link it from 00-DESIGN. (Records 0001-0004 for the user-management entity graph / team-target / role-defs / tag dictionary were backfilled retroactively; going forward the ADR is authored with the decomposition.)","id":"mx-3df6fe"}
{"type":"decision","classification":"tactical","recorded_at":"2026-06-07T22:55:58.284Z","evidence":{"commit":"d4b475a0d2c8fd2aa27ee67496c73446940028c5"},"title":"Document the AI-workflow tooling stack in-repo and extract a portable deep-review template","rationale":"The flow guide described the process but not the tooling; the deep-review skill is gitignored so its method needed a committed, shareable home; and crediting upstreams is part of the AI-assisted-engineering consulting story.","id":"mx-6369df"}
{"type":"decision","classification":"tactical","recorded_at":"2026-07-06T15:42:27.182Z","evidence":{"commit":"e915808a46622468675166134cc4bb04177ea0c4"},"title":"User-directory slice: 2-way split; Slice 2 = pure-search UserDirectory port + optional Keycloak-admin module (grill-me settled 2026-07-06)","rationale":"Grilled the Phase-7 user-directory plan-of-record into TWO slices (§2a). SLICE 1 (separate, self-evident): server-side listAll*-killer filters — ?subject on /users, ?targetType/?targetId on /teams, produces/Accept spec fix, bootstrap displayName upsert; one deployable, no external dep, ~4 tickets. SLICE 2 (designed here): the Keycloak-admin UserDirectory port. 8 pinned decisions: (1) split along feature-vs-filters. (2) PURE SEARCH read-model — provisioning explicitly OUT of scope (SPA provisions on select via existing POST /users); the SPI is search-only so consumers can implement provisioning differently. (3) Port UserDirectory.search(String query,int limit)->List<DirectoryUser> + DirectoryUser(subject,displayName) record + NoOpUserDirectory default in opa-abac-spring-security (mirrors ResourceOwnershipResolver); Keycloak impl in its OWN optional module (opa-abac-keycloak-directory) auto-wired like Resilience4j (@ConditionalOnClass+@ConditionalOnProperty, NoOp fallback -> lean-starter preserved). (4) Keycloak auth = dedicated catalog-directory confidential client, serviceAccountsEnabled, client_credentials grant, granted ONLY realm-management->view-users on catalog-demo (least privilege, no human credential, separate trust role from catalog-gateway). (5) GET /api/v1/users/search?q=&limit= on the user-service UserApi (spec-first), bearer-only; response a BOUNDED PLAIN LIST {items:[{subject,displayName}],limit} NOT a fake Page envelope. (6) keycloak:8888 in-network URL is a PRIVATE impl/config detail of the module — port+endpoint stay URL-agnostic (encapsulation); ADR notes KC_HOSTNAME_ADMIN_URL=localhost:28888 is a console-rewrite red herring, not the REST path. (7) authz: bearer-only (consistent w/ ungated GET /users), disclosure TYPE-BOUNDED to {subject,displayName} in the DTO so no impl can widen it; the real gate stays on team:add-member; documented as a deliberate decision (not an ungated-enumeration finding). (8) fail-closed: 7 edges pinned — unreachable/timeout/5xx->[], grant-fail->[]+WARN, blank q->[] WITHOUT calling Keycloak, limit clamped default 20 hard-max 50, null username->fall back to subject, module-off->NoOp []; BINARY port (empty-or-results), outage vs genuine-empty distinguishable ONLY in logs — SPA renders both identically as a deliberate NO-ORACLE security property (never leak backend state/realm size to UI). §2a: Slice 2 passes as ONE purely-additive slice (trips (b) multi-deployable + borderline (c) ticket count, but CLEAN on the dangerous (a) remove-and-replace and (d) unenumerable-consumers); sequenced BACKEND-FIRST (newman-provable before SPA rewrite); SPA-split (2a/2b) held in reserve if decomposition >~6 tickets. NET-NEW: zero existing Keycloak-admin code in the repo; KC 26.3.2, realm catalog-demo.","id":"mx-698e64"}
{"type":"decision","classification":"tactical","recorded_at":"2026-06-11T06:40:01.701Z","evidence":{"commit":"5d316252489d4c300d33dd1761dda753f4a1bb98"},"title":"Phase-③ stays bare-prompt for single-repo slices; phase-② gates are committed scripts","rationale":"All 8 shipped slices were single-repo with no resume/re-entry need; the AUTONOMOUS-IMPLEMENT-SKILL-TEMPLATE's own guidance says the skill wrapper only earns its keep for multi-repo, resume-heavy, or enforced-escalation slices. Deterministic committed scripts beat prose gates (code is deterministic; language interpretation is not).","id":"mx-70582b"}
{"type":"decision","classification":"tactical","recorded_at":"2026-06-03T23:47:58.898Z","evidence":{"commit":"ad7ac716cc195509e5a1df59dae8ce2e260ed34a"},"relates_to":["mx-15ee3e","mx-666644"],"title":"Phase 5 data-filtering planned: partial-eval (compile→DNF residual→JPA Specification) + batch, fail-closed, abstract OpaClient methods","rationale":"Phase 5 (the advanced-library slice) is planned+decomposed in docs/to-do/planning/DATA-FILTERING/ (full package: index, 00-DESIGN, 01-DECOMPOSITION T1-T7, AUTONOMOUS-IMPLEMENTATION-PROMPT, 10-QA, 7 STATUS stubs) mirroring the four shipped slices 1:1. SCOPE (confirmed w/ maintainer): partial-eval + batch BOTH, NOT hierarchical ancestor-walk (that filters by ancestors; this slice filters by the row's OWN tags — the load-then-check CategoryAuthorizer stub stays). KEY DECISIONS: (1) Two ADDITIVE OpaClient methods — compile(AbacContext)->PartialResult via OPA Compile API (POST /v1/compile, query 'data/<type>/filter == true', unknowns=['input.resource'], resource OMITTED from input) and allowAll(List<AbacContext>)->List<Boolean> via a per-type 'bulk' rule. Make them ABSTRACT, not default methods, so a custom OpaClient cannot silently inherit a fail-OPEN filter. allow() + the @OpaPreAuthorize path stay byte-for-byte unchanged. (2) Neutral residual model in core (Spring-free): PartialResult{decision ALLOW_ALL|DENY_ALL|CONDITIONAL, List<Conjunction>} = DNF (OR-of-ANDs); Condition{path, Operator EQ|NEQ|IN|CONTAINS, value}. Mapping: compile result absent/{}->ALLOW_ALL; result.queries==[]->DENY_ALL; non-empty->CONDITIONAL. (3) ResidualSpecificationFactory in spring-data translates residual->Specification<T> over the tags JSONB (GIN-indexed since domain-model slice): EQ/IN via jsonb_extract_path_text(tags,'k'); CONTAINS (array tag) via the ? existence op jsonb_exists(tags->'k','v') — mirrors the TAG-DICTIONARY resource_tag_values scalar-vs-array normalize (mx-666644); intrinsic path (e.g. categoryId)->root.get(). ALLOW_ALL->no predicate; DENY_ALL->cb.disjunction() (always false). Postgres-only dialect (seam noted, second dialect not built). (4) FAIL-CLOSED is the load-bearing invariant: compile error->DENY_ALL (empty page); batch error->all-false; an UNSUPPORTED residual (operator outside the closed set / a non-input.resource ref) -> DENY_ALL, or with allowlistFallback=on -> exact batch re-check over a recognized-conjunct pre-filter. NEVER 'return everything' on error. The closed small operator set is deliberate (narrow-but-correct > wide-but-wrong: a mistranslated predicate = a silent data leak). (5) Three-layer separation: Compile-API+DNF in core, JSONB->Criteria in spring-data, the rego 'filter'/'bulk' entrypoints in policy. The differentiator (mx-15ee3e): the one comparable OSS project opa-data-filter is unmaintained. Lead-in still distinct from Phase-7 ReBAC-in-Rego (mx-723b5c). Planning branch: feature/void3110/plan-data-filtering.","id":"mx-c39a0a"}
{"type":"decision","classification":"tactical","recorded_at":"2026-06-04T00:11:53.646Z","evidence":{"commit":"41504cedb649eb4cf15517526294b5c1f61704af"},"relates_to":["mx-15ee3e","mx-723b5c"],"dir_anchors":["docs/architecture/adr","docs/to-do/planning"],"title":"Roadmap expanded: action enrichment = Phase 6 (affordance map via response advice + action registry + x-implements DTO marker); two ADRs pinned (0005 partial-eval, 0006 three-layer)","rationale":"Roadmap grown along two routes (maintainer ask): tech topics + user stories, anchored by ADRs. KEY ADDITIONS: (1) ACTION ENRICHMENT as a new Phase 6 (bumps publish 6->7, ReBAC 7->8), planning note docs/to-do/planning/ACTION-ENRICHMENT (direction set, not yet decomposed; sequenced after Phase 5 because it is the FIRST consumer of the Phase-5 batch primitive OpaClient.allowAll). Enrichment = attach a {action:allowed} affordance map to a returned resource/page so a UI renders the right buttons. It is AFFORDANCE, NOT enforcement (enforcement = ADR 0006 three layers). Decided shape: a response decorator/ResponseBodyAdvice (automatic, not per-endpoint boilerplate); action source = a per-resource-type ACTION REGISTRY (generalizes the source platform's ActionName enum + ResourceTypeActions; doubles as a validation allowlist; superior to role-permissions-only because it can report delete:false for an ungranted verb). (2) THE KEY MECHANISM for fitting enrichment to OpenAPI codegen: stamp resource DTOs with an x-implements vendor extension so the org.openapi.generator 'spring' generator makes the generated POJO implement a library marker interface (e.g. Enrichable) — confirmed against the source platform's external-credentials DTO which stamps java.lang.AutoCloseable via 'x-implements: [java.lang.AutoCloseable]' + 'x-closeable: true'. The advice detects Enrichable returns, batch-evals, injects _actions. App opts in with ONE x-implements line in its spec — no DTO hand-editing/post-processing. Open Q held for Phase-6 planning: the envelope shape (inline _actions vs a wrapping Authorized<T>). (3) USER STORIES route: docs/to-do/planning/USER-STORIES.md — the PRODUCT lens over the tech roadmap; personas (viewer/editor/owner-admin/super-admin/integrator) + epics A-F each tagged to its delivering phase (A=P3 enforcement, B=P4 team roles, C=P4.5 tags, D=P5 list filtering, E=P6 enrichment, F=P7 publish). (4) ADRs authored UP FRONT per our own convention (mx — the ADR-in-decomposition convention): 0005 partial-eval->JPA Specification (pins the DATA-FILTERING fork: compile API->DNF residual->Specification over tags JSONB, fail-closed, abstract-not-default OpaClient methods), 0006 the three-layer enforcement model (gateway coarse -> app @OpaPreAuthorize/load-then-check -> DB partial-eval; THE PRINCIPLE: the app NEVER trusts the gateway, re-decides every single-resource return against the loaded resource to stop horizontal privilege escalation on a guessed ID — the gateway is defense-in-depth, not the decision). 0005/0006 are the first ADRs written up-front (0001-0004 were retroactive). Action-enrichment ADR PENDING (written with its decomposition). Branch: feature/void3110/plan-data-filtering (continuation).","id":"mx-d360b0"}
{"type":"failure","classification":"tactical","recorded_at":"2026-06-03T00:34:05.528Z","evidence":{"commit":"a2c2302d9e49c889318149926385468c18291403"},"relates_to":["mx-a1fa1d"],"description":"The per-ticket workflow runs a clean-room/build check with 'git add -A' (or 'git add -A' to stage a diff for scanning) and ALSO records Mulch with 'ml record … && ml sync'. ml sync stages and commits whatever it considers part of the expertise update — but in practice it commits EVERYTHING currently staged, not just .mulch/. So a 'mulch: update expertise' commit silently swept in the ticket's source/docs/a git-mv folder rename three separate times this run, violating the hard rule that Mulch sync commits must touch .mulch/ ONLY (and mixing code into a commit titled 'mulch: …').","resolution":"Before 'ml sync', ensure the index contains ONLY .mulch/ — run 'git restore --staged .' (unstage everything) then either let ml sync stage .mulch itself, or 'git add .mulch/' explicitly. If a mixed 'mulch: …' commit already happened, recover with: git reset --soft HEAD~1; git restore --staged .; git add .mulch/; git commit -m 'mulch: …'; then git add <the ticket's real paths> and commit the source separately. Order the per-ticket steps so the clean-room scan uses 'git add -A' + 'git diff --cached' + 'git restore --staged .' (leaving a CLEAN index) BEFORE doing the Mulch record/sync, so ml sync can't capture staged source.","id":"mx-d8a173"}
{"type":"failure","classification":"tactical","recorded_at":"2026-07-06T15:55:32.071Z","evidence":{"commit":"e46afa2451d39f01917b67577cb5f504865b500b"},"description":"verify-package.sh's clean-room scan (scripts/planning/verify-package.sh [3]) runs grep -rnEi (CASE-INSENSITIVE) with a GENERIC pattern that includes '/Users/[a-z]' (to catch leaked mac home-dir paths like /Users/dmitry). Case-insensitively this ALSO matches the public REST path fragment '/users/<lowercase-letter>' — so a decomposition package that writes a literal API path like '/users/search' or '/users/by-subject' trips a FALSE-POSITIVE clean-room violation and the gate fails. Shipped packages passed only because their user/team paths are always '/users/{id}' or '/teams/{teamId}' — the char after the collection is '{', not [a-z], so no match. Hit while decomposing DIRECTORY-QUERY-FILTERS (Slice 1 of user-directory).","resolution":"Reword the prose so the collection segment is NOT immediately followed by a lowercase letter: write 'a search endpoint under /api/v1/users' or 'a by-subject sub-path under /api/v1/users' instead of the bare '/users/search' / '/users/by-subject/{sub}'. The path is still clear; the /users/<lower> literal is avoided. (Do NOT weaken the GENERIC pattern — /Users/[a-z] earns its keep catching real home-dir leaks; the case-insensitive collision is the price.) Confirmed: after rewording, verify-package.sh DIRECTORY-QUERY-FILTERS => PACKAGE OK.","id":"mx-e621ea"}
{"type":"reference","classification":"observational","recorded_at":"2026-07-19T19:46:32.747Z","evidence":{"commit":"31ebb1a0b3ca8b6c789190a8097ee9efd8e74589"},"name":"Agentic-OS hardening research (2026-07-19): verified proposal set + top-5","description":"Multi-agent research pass mapped the 'agentic OS' idea onto the methodology (vault note: 01-Learning/Agent-Patterns/Agentic-OS-Research-and-Methodology-Hardening.md — senses taxonomy, 12-row OS-primitive mapping table, 18 judge-refined proposals, 13 rejected-with-reasons). CORE DIAGNOSTIC: all 5 lenses found the same gap shape — the method's OS is 'stated, not enforced' (rings/ABI/audit live in prose; .claude/settings.json is {}). TOP-5 DO-FIRST: (1) ring-0 deny rules in settings.json ~30min; (2) skeleton-fidelity conformance gate in verify-package.sh (3-lens convergence) ~2-3h; (3) mandatory 'Changed shared mechanisms' declaration + LSP consumer-enumeration artifact in the slice-sizing gate (the only clean adopt; mechanizes the B4 lesson) ~1 session; (4) kernel-written checkpoint evidence blocks replacing hand-pasted test summaries ~2-3h; (5) thin rig-doctor + verify-run-state.sh (closes corp-identity-in-public-repo tail risk) ~3h. Occasion-driven rider: OPA PreToolUse gate governing the loop itself = highest consulting-value item, build WITH its write-up. STRATEGIC GAP (queue as own task): every proposal points OPA inward; the outward move — an agent-authz example/module (Spring Boot PEP for MCP/agent tool calls, dual-identity principal=human/actor=agent) — fuses the ABAC + AI-engineering tracks into one portfolio artifact; IdP consumer is the natural first user.","id":"mx-9f5716"}
{"type":"decision","classification":"tactical","recorded_at":"2026-07-27T01:04:47.369Z","evidence":{"commit":"52baade5e0751023cffbc3cb20a9758c313f175b"},"title":"Phase 9 (agent tool-call authz): the 8 settled forks (grill-me 2026-07-27)","rationale":"Grilled with the maintainer 2026-07-27; full design brief + re-verification in the vault note Agent-Tool-Call-Authorization-Direction.md. SETTLED: (1) scope fence ABOVE the validated token — transport/OAuth OUT (mcp-security's job; principal = validated token's sub/claims), tool+argument ABAC IN, RAG = Phase-10 lane; PREREQ: diff FINAL vs RC on authz/tools sections when the 2026-07-28 spec text publishes. (2) PEP legs: server-side @McpTool AuthorizationManager-style gate FIRST (corp-shape mirror), host-side ToolCallingManager decorator second; engine core leg-agnostic; identity rides ToolContext never ThreadLocals. (3) identity: RFC 8693 act semantics canonical, normalized by a DelegationChainExtractor SPI; demo tier = plain custom claim on stock Keycloak; Keycloak 26.8 native-act showcase documented, rig NOT pinned to nightly. (4) agent model: principal role = ceiling (existing RoleDefinitionSupplier unchanged) ∩ agent capability profile that only narrows (new AgentCapabilitySupplier, same tri-state doctrine, turn-scoped memo); intersection computed in Rego; NO standing agent grants; agents in UserDirectory as read-model only. (5) input schema: two-layer (tool-gate powers roster/tools-list filtering via allowAll; target-gate feeds EXISTING per-type policies UNCHANGED with the pre-narrowed role_definition); additive input.subject.actor/chain; published versioned input-schema spec = the standard-setting deliverable. (5b) topology: two-step short-circuit (tool-gate always; resolve+target-gate only on allow AND domain-addressing tool); deny = structured advisory tool-error naming the layer, 403 only when scope-expressible. (6) packaging: example-first in a new example-mcp-server over the catalog domain; extract OPTIONAL module opa-abac-agent (keycloak-directory packaging template) as the slice EXIT criterion; publication rides the next release train. (residual 8) roster = call-time-only enforcement in v1, listChanged documented follow-up. (residual 10) tools declare action/category/tags statically; ToolCallClassifier SPI ships contract-only (most-restrictive-on-ambiguity), no impl in v1. PINNED (not forks): fail-closed tri-state on every new edge, per-layer kill-switches, HITL OUT of v1, smallest-honest-demo = scripted no-LLM MCP client, module extraction = exit not entry.","id":"mx-16cf80"}
