{"type":"pattern","classification":"tactical","recorded_at":"2026-05-31T14:31:05.863Z","evidence":{"commit":"74d50ba5b6160565ab5d70b8cce9bd235442f5e7"},"name":"ABAC input model: Subject/Action/Resource/Environment","description":"Model the OPA decision input as four parts — Subject (who: id, roles, attributes), Action (what: a string verb), Resource (on what: type, id, attributes), Environment (circumstances: map). Keep this record framework-agnostic in opa-abac-core so the Spring modules and any client serialize the same shape as OPA 'input'. Pluggable extraction (JWT claims -> Subject) lives in the spring-security module, never in core.","id":"mx-2c6374"}
{"type":"convention","classification":"tactical","recorded_at":"2026-05-31T14:31:06.216Z","evidence":{"commit":"74d50ba5b6160565ab5d70b8cce9bd235442f5e7"},"content":"OPA authorization helpers MUST fail closed: any error talking to OPA, any timeout, any unparseable response, or any ambiguity denies the request. Never default to allow on failure. The OPA client should treat a non-200 / connection error as 'deny' and surface it, not swallow it into 'true'.","id":"mx-926c85"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-05-31T14:31:06.518Z","evidence":{"commit":"74d50ba5b6160565ab5d70b8cce9bd235442f5e7"},"name":"Two-layer authorization: coarse gateway + fine app check","description":"Split authorization into a coarse gateway layer (e.g. APISIX calling OPA to check the request maps to a known route + the subject holds the permission CATEGORY) and a fine application layer (the service runs an ABAC check on the concrete resource's attributes). The gateway is an explicit allow-list (default deny): every new endpoint path needs its own validator + allow rule, or it is rejected regardless of role. The app layer checks individual action permissions against the loaded resource. Both must agree; a new action needs updating in both places.","id":"mx-7130a1"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-05-31T14:31:06.858Z","evidence":{"commit":"74d50ba5b6160565ab5d70b8cce9bd235442f5e7"},"name":"Hierarchical resource authorization (inherit from parent)","description":"When resources form a tree (Catalog -> Category -> Product), a child can inherit authorization from its parent. Resolve the parent chain and let a grant on an ancestor satisfy a check on a descendant. Make this an explicit, opt-in 'hierarchical' mode on the authorization API rather than implicit, so callers know when ancestor grants apply.","id":"mx-506f5c"}
{"type":"failure","classification":"tactical","recorded_at":"2026-05-31T16:35:31.008Z","evidence":{"commit":"9e39a1304f1d87c088872c0e0e96c01857d2594a"},"description":"Local APISIX container-pool rig (deploy.sh) failed to start in several non-obvious ways: (1) APISIX 3.x reads etcd config under deployment.etcd, not a top-level 'etcd:' key — a top-level key is silently ignored, APISIX falls back to 127.0.0.1:2379, logs 'all etcd nodes are unavailable', and the container exits. (2) bitnami/etcd images are gone (Bitnami sunset their free Docker Hub catalog) -> 'manifest unknown'. (3) Host port clashes with a running portal podman-machine gvproxy: :9080 and the 18081/18082 range are already bound. (4) Killing Postgres mid-session (e.g. compose down --remove-orphans on one of the shared-project compose files) makes pods return 503 with java.net.UnknownHostException: opa-abac-postgres.","resolution":"(1) Put etcd under deployment.etcd in apisix/config.yaml. (2) Use quay.io/coreos/etcd:v3.5.x with explicit --listen-client-urls/--advertise-client-urls. (3) Use proxy port 9085 and pod base port 28080 (pods 28081+) to dodge the podman-machine. (4) All three composes share name: opa-abac-example so they share opa-abac-example_default; never run 'compose down --remove-orphans' on a single file — it deletes the others' containers. Pods auto-recover (HikariCP reconnect) within seconds once Postgres is back. Bonus: enable response-rewrite plugin and set X-Upstream-Addr=$upstream_addr on the route to prove round-robin.","id":"mx-85c842"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-05-31T17:05:38.922Z","evidence":{"commit":"c31548762f618e232931c974fbc4a17ac31d9fe6"},"name":"Trace the APISIX->OPA->app->DB chain into Jaeger locally","description":"Phase B of the example rig adds end-to-end distributed tracing. Pattern that worked: (1) Jaeger v2 + embedded Badger (jaegertracing/jaeger:2.17.0, --config=jaeger-config.yaml) for persistent traces with no JVM (avoids aarch64 SIGILL of ES/OpenSearch). (2) APISIX opentelemetry plugin via plugin_attr.opentelemetry.collector.address=jaeger:4318, enabled per-route with {\"opentelemetry\":{\"sampler\":{\"name\":\"always_on\"}}}. (3) OTEL Java agent BAKED INTO the app image (ADD opentelemetry-javaagent.jar /opt/otel/agent.jar) and attached per-pod via -javaagent + OTEL_SERVICE_NAME/OTEL_EXPORTER_OTLP_ENDPOINT=http://opa-abac-jaeger:4318/OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf. (4) OPA with distributed_tracing.type=grpc,address=jaeger:4317 emits its own decision spans. Verified: GET /api/services returns [apisix, catalog-management-service, opa, jaeger]; one APISIX trace nests gateway->app->JPA/DB spans. Note: APISIX opa plugin does NOT propagate trace context into the OPA call, so OPA spans are a separate service, not nested — expected. All host ports for Jaeger(16686/4317/4318)/OPA(8181) clash with a running portal podman-machine, so remap host side to 2xxxx; in-network DNS (opa-abac-jaeger:4318, opa:8181) is unaffected.","files":["infra/compose.jaeger.yaml","infra/compose.opa.yaml","infra/apisix/config.yaml","infra/apisix/init-routes.sh","example-catalog-management-service/Dockerfile","deploy.sh"],"id":"mx-05ebc1"}
{"type":"failure","classification":"tactical","recorded_at":"2026-05-31T17:17:54.853Z","evidence":{"commit":"ac1df60eb5d4c35a39c90bd42f4cf4352efed3d2"},"description":"Wiring APISIX openid-connect to a Keycloak container hit two non-obvious failures: (1) 'unknown plugin [openid-connect]' on the route PUT — APISIX only loads plugins listed in config.yaml's top-level 'plugins:'; adding openid-connect there requires RECREATING the apisix container (editing the file isn't enough, and curl -sf swallows the 400 so the route silently lacks the plugin). (2) Keycloak issuer mismatch: Keycloak is hostname-aware, so the discovery doc + token issuer differ by how it's reached — in-network http://keycloak:8888 vs host http://localhost:28888. APISIX discovers + validates IN-NETWORK (issuer keycloak:8888), so a token minted against localhost:28888 has a mismatched issuer and is rejected. Tokens must be minted in-network or via the browser redirect flow.","resolution":"(1) After changing config.yaml plugins, force-recreate apisix (docker compose up -d --force-recreate apisix) before seeding routes; don't trust curl -sf (drop -f or inspect the body). (2) Pin KC_HOSTNAME_URL=http://keycloak:8888 + KC_HOSTNAME_STRICT=false so the in-network issuer is stable; for testing, mint tokens in-network via 'docker run --network opa-abac-example_default curlimages/curl ... http://keycloak:8888/realms/catalog-demo/protocol/openid-connect/token'. Verified: no-token->302, valid in-network token->200, bad->401. ENABLE_OIDC gate keeps it opt-in. Service does NO JWT check (gateway-only auth, Phase 2).","id":"mx-c5a27d"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-05-31T17:26:50.249Z","evidence":{"commit":"0cff5cb278799a74167f3508e869cacc239fd308"},"name":"APISIX gateway plugin ordering for auth chain (oidc->enrich->opa)","description":"On an APISIX route that does OIDC + identity enrichment + OPA, plugin execution order matters and is controlled by _meta.priority (HIGHER runs first), not by JSON key order. Proven chain (matches portal): openid-connect(2599) -> serverless-pre-function enricher(2500) -> opa(2000) = authenticate, then inject identity headers from the validated JWT, then authorize. WITHOUT an explicit override, serverless-pre-function defaults to a very high priority and would run BEFORE openid-connect (no validated token yet) — so you MUST pin its _meta.priority below openid-connect. The enricher is a small Lua that base64url-decodes the JWT payload (already verified by openid-connect; it only READS claims) and sets X-User-Id (sub) / X-Username (preferred_username) via core.request.set_header. Build the Lua-in-JSON via a Python helper (json.dumps) to avoid bash escaping bugs. Keycloak omits 'sub' from the access token unless you add an oidc-sub-mapper protocolMapper to the client in the realm export (access.token.claim=true).","files":["infra/apisix/enricher-plugin.py","infra/apisix/init-routes.sh","infra/keycloak/realm-export.json"],"id":"mx-265f96"}
{"type":"decision","classification":"tactical","recorded_at":"2026-05-31T17:30:58.639Z","evidence":{"commit":"bd39c9f2fb39bfdbf1dbc1d223f69ac6b85648d1"},"title":"Gateway-side identity/enrichment is demo scaffold; the library does real extraction","rationale":"The example rig's gateway does OIDC termination (APISIX openid-connect) and a Lua serverless-pre-function that decodes the JWT and injects X-User-Id/X-Username for OPA. This gateway-side identity handling is DELIBERATELY a throwaway demo so the topology is visible/traceable end-to-end NOW, before the library exists. It is NOT the target architecture. The project thesis (POC-ROADMAP) is to be Spring-NATIVE with no gateway 'mediator': real subject/attribute extraction belongs in the library — opa-abac-spring-security's pluggable Authentication->AbacContext extractor (see mx-ea7974), feeding OpaAuthorizationManager / @OpaPreAuthorize, with the service doing its own fine-grained ABAC (two-layer model mx-7130a1). So in Phase 3 the demo Lua enricher gets removed; the app validates the JWT and extracts AbacContext itself. Keep the gateway doing only coarse auth (authn + a coarse OPA route check); push fine-grained, attribute-level decisions into the app via the library. Don't let the demo enricher calcify into the design.","id":"mx-cbca87"}
{"type":"decision","classification":"tactical","recorded_at":"2026-05-31T18:31:30.453Z","evidence":{"commit":"de67c53bfbf9585ac40ae1d4eacd02d1f79b135a"},"relates_to":["mx-506f5c"],"title":"Base-entity layer: opa-abac-spring-data, plain UUID ids, tags on the secure base, mutate() as the safe write default","rationale":"Foundational domain-model design for the clean-room starter (the DOMAIN-MODEL-FOUNDATION slice). Decisions: (1) the reusable base stack (AbstractAuditableEntity, AbstractSecuredEntity, ResourceTags + converter, LockableJpaRepository, AbstractCrudService) lives in opa-abac-spring-data — it already depends on opa-abac-core + spring-boot-starter-data-jpa, and opa-abac-core MUST stay Spring-free, so JPA/Spring annotations cannot live in core. (2) Trim typed-id value objects: use a plain UUID id. Our ABAC layer (AbacContext, AbacDataObject) is String/UUID-based and no planned feature consumes a typed id, so a wrapper is pure ceremony; reintroducible additively later. (3) Tags live ONLY on the SECURE base (AbstractSecuredEntity), never the plain one, and the secure base implements AbacDataObject so a secured entity is authorizable out of the box (the fix for 'secure = just a marker'). (4) JSONB tags via @JdbcTypeCode(SqlTypes.JSON) + an AttributeConverter (idiomatic on Hibernate 6.4) rather than a hand-rolled UserType. (5) Timestamps are OffsetDateTime + @TimeZoneStorage(NATIVE) -> timestamptz, matching the existing CatalogEntity.createdAt. (6) Concurrency: expose getById (unlocked) AND getByIdForUpdate (pessimistic) explicitly, and make mutate(id, fn) (lock+mutate+save) the safe-by-default write path — the generalized fix for inconsistent locking discipline that causes stale-@Version lost-update races. Verify serialization with a latch-forced concurrency IT on real Postgres (not H2). @AutoTag auto-tagging is deferred (tags set explicitly for now).","id":"mx-fe5c67"}
{"type":"failure","classification":"tactical","recorded_at":"2026-05-31T18:51:59.263Z","evidence":{"commit":"3ac05e320221e4d53ac610cfb33e2b0359b4bbe8"},"dir_anchors":["example-catalog-management-service/src/main/java/dev/dmitriikonovalov/example/catalog/domain"],"description":"Spring Data JPA auditing (@CreatedDate/@LastModifiedDate) on an OffsetDateTime field throws 'Cannot convert unsupported date type java.time.LocalDateTime to java.time.OffsetDateTime' on save. The default auditing date source emits LocalDateTime and its DateConvertingAuditableBeanWrapper cannot target OffsetDateTime. Hit when the base entities standardized on OffsetDateTime/timestamptz.","resolution":"Supply a DateTimeProvider bean returning Optional.of(OffsetDateTime.now()) and reference it via @EnableJpaAuditing(dateTimeProviderRef=\"...\"). Auditing then uses the OffsetDateTime as-is, no conversion. (Same applies to any non-LocalDateTime audit timestamp type.)","id":"mx-5fb01d"}
