/**
 * Spring Data integration for OPA-backed ABAC.
 *
 * <p>Planned surface (built up in later phases):
 * <ul>
 *   <li>{@code AbacSpecification} / {@code AbacSpecificationFactory} — OPA partial evaluation
 *       translated into a JPA {@code Specification} for row-level data filtering.</li>
 * </ul>
 */
package dev.dmitriikonovalov.opaabac.data;
