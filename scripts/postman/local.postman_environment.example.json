{
  "id": "catalog-local-env",
  "name": "Catalog Management (Local rig)",
  "values": [
    { "key": "base_url", "value": "http://localhost:9085", "enabled": true,
      "description": "APISIX proxy (host). Not 9080 — gvproxy holds that on this host." },
    { "key": "collection_base_url", "value": "{{base_url}}/api/v1", "enabled": true,
      "description": "The catalog API is versioned: /api/v1/..." },

    { "key": "realm", "value": "catalog-demo", "enabled": true },
    { "key": "client_id", "value": "catalog-gateway", "enabled": true },
    { "key": "client_secret", "value": "catalog-gateway-secret", "enabled": true },
    { "key": "username", "value": "demo", "enabled": true },
    { "key": "password", "value": "demo", "enabled": true },

    { "key": "keycloak_token_url",
      "value": "http://keycloak:8888/realms/catalog-demo/protocol/openid-connect/token",
      "enabled": true,
      "description": "IN-NETWORK issuer. APISIX validates issuer keycloak:8888, so the token must be minted in-network. run-tests.sh does this and injects access_token; the Auth folder works only if newman itself runs on opa-abac-example_default. See docs/guides/E2E-TESTING.md." },

    { "key": "access_token", "value": "", "enabled": true,
      "description": "Injected by run-tests.sh (--env-var access_token=...). Leave blank in the file." },

    { "key": "catalog_id", "value": "", "enabled": true,
      "description": "Captured at runtime by the Catalog folder." },
    { "key": "category_id", "value": "", "enabled": true,
      "description": "Captured at runtime by the Category folder." },
    { "key": "product_id", "value": "", "enabled": true,
      "description": "Captured at runtime by the Product folder." }
  ],
  "_postman_variable_scope": "environment"
}
