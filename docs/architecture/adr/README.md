---
tags:
  - type/index
  - area/abac
  - area/user-service
---

# Architecture Decision Records (ADRs)

> The **why** behind structural decisions, kept as small immutable records. Each ADR captures one
> decision: its context, the options weighed, the choice, and the consequences we accepted. Unlike the
> prose architecture docs ([[DOMAIN-MODEL]], [[TWO-LAYER-AUTHORIZATION]]) which describe how things work
> *now*, an ADR is a dated snapshot of *why a fork was taken* — it doesn't get rewritten as the system
> evolves; it gets **superseded** by a later ADR.

## Format

Lightly-MADR. Each record has: **Status · Context · Decision · Considered options (with why-rejected) ·
Consequences**. Filenames are `NNNN-short-title.md` (zero-padded, monotonic). A decision is never edited
once `Accepted` — to change it, write a new ADR that supersedes it and flip the old one's status.

## Status values

`Proposed` → `Accepted` → (`Superseded by [[NNNN-...]]` | `Deprecated`).

## When to write one (the convention, going forward)

ADRs are **part of the decomposition process**, not an afterthought. When planning a feature
(`docs/to-do/planning/<FEATURE>/`), a **structural decision** gets an ADR — written *up front*, as the
fork is decided, and linked from the feature's `00-DESIGN`. A decision is "structural" when it would be
expensive to reverse or surprising to a future reader: a schema or authority shape, a module/service
boundary, where a check is evaluated (app vs. policy), an additive-vs-breaking choice, a "we deliberately
did **not** do X" with real alternatives weighed.

Why up front, not after: a feature's `00-DESIGN`/`01-DECOMPOSITION` are *living* docs — they get
rewritten and then `git mv`'d to `implemented/` on ship, so the rationale buried in them drifts or moves.
An ADR is *immutable* — it pins the decision and its rejected alternatives at a point in time. Routine
implementation choices (naming, file layout, which test library) do **not** need an ADR; reach for one
only when you catch yourself writing a "considered & rejected" list worth keeping.

> Records 0001–0004 were written **retroactively** for the Phase-4/4.5 user-management work (the decisions
> were sound, the docs just hadn't been pinned). From here, the ADR is authored *with* the decomposition —
> **0005 and 0006 are the first records written *up front*** (0005 pins a Phase-5 fork as part of the
> [[DATA-FILTERING]] decomposition; 0006 pins a cross-cutting model that previously lived only in prose).

## Index

| # | Title | Status | Area |
|---|-------|--------|------|
| [0001](0001-user-management-entity-graph.md) | User-management entity graph & layered service structure | Accepted | user-service |
| [0002](0002-team-and-team-target-indirection.md) | Team + team-target: the resource→authority indirection | Accepted | user-service · abac |
| [0003](0003-role-definitions-role-not-grant.md) | Role definitions: role ≠ grant, system + team-scoped, app-resolved | Accepted | user-service · abac |
| [0004](0004-dynamic-tag-dictionary.md) | The dynamic tag dictionary: three layers, global + team, match-in-Rego | Accepted | user-service · abac |
| [0005](0005-partial-eval-to-jpa-specification.md) | Partial evaluation → JPA `Specification`: residual conditions in the SQL `WHERE` | Accepted (implemented) | spring-data · abac · opa |
| [0006](0006-three-layer-enforcement-model.md) | The three-layer enforcement model: gateway → app → DB | Accepted | abac · opa · spring |
| [0007](0007-coarse-grained-permission-categories.md) | Coarse-grained permission categories (READ/WRITE/TAG/GRANT) + the five-tier ceiling model | Accepted (implemented) | user-service · abac · spring |
| [0008](0008-hierarchical-resource-authorization.md) | Hierarchical (N-level ancestor) resource authorization: chain-in-input, opt-in inheritance, `ltree`/CTE resolver SPI, fail-closed | Accepted (implemented) | abac · opa · spring-data |
| [0009](0009-tag-requirement-subject-side.md) | The tag access requirement is subject-side (on the role), not resource-side: AWS-IAM-style, fail-closed, row-filter-native; Keycloak-style resource-gated clearance deliberately deferred | Accepted | abac · opa · spring-data |
| [0010](0010-hierarchy-aware-list-filter.md) | Hierarchy-aware list filter — the `subtreeSpec` composition: root-only subtree roots, the `subtreeOf` SPI (predicate-not-id-set), deny-overrides-as-SQL (`abac_deny`), the hierarchy-aware allowlist batch; extends ADR 0008 (took the "0009" 0008 had reserved) | Accepted (implemented) | abac · opa · spring-data |
| [0011](0011-error-contract-problem-json.md) | Error contract: RFC-7807 `application/problem+json` (minimal additive superset, no hosted type registry) + a library-owned, app-extensible `ApiErrorCode` vocabulary (interface + base enum, semantic granularity, typed in the OpenAPI schema); clean replacement of `ApiError` | Accepted (implemented) | api · architecture · spring |
| [0012](0012-pagination-envelope.md) | Pagination envelope composed with ABAC list filtering: exact-count `{count, page, perPage, items}` on all four query paths, the additive `Pageable`/`Page<T>` seam (wire shape spec-owned), strict 0-based params (no clamping, past-the-end = 200+empty), determinism by construction (fixed `createdAt,id` order + the unsorted-`Pageable` guard), envelope-everywhere/authz-nowhere | Accepted (planned) | api · spring-data · abac |
| [0013](0013-attribute-rich-pre-authorization.md) | Attribute-rich pre-authorization: the split `AbacResourceResolver`/`AncestorChainSupplier` SPI (app resolves the instance, starter composes ancestors), the gate's full per-instance decision (tags + ancestors, role on the governing root — mirrors `HierarchicalAuthorizer`), split fail-closed semantics (instance failure → deny; ancestor failure → direct-only), the write-through request-scoped cache, version binding via the one JPA `@Version` (reads return the snapshot; mutations guard → `409 STATE_CONFLICT`), the `opa.abac.resource-resolution.enabled` kill-switch; central class-keyed rule registry explicitly rejected | Accepted (planned) | abac · architecture · spring |
| [0014](0014-supplier-outage-error-distinct.md) | Role-source outage is error-distinct from no-role at the `RoleDefinitionSupplier` SPI: a tri-state contract (`Optional.of`=resolved · `Optional.empty()`=authoritative no-role→realm fallback · **throw `RoleResolutionException`**=outage→deny/no-widening), an unchecked exception in `opa-abac-core`, the strict HTTP classification (only `204`→fallback, only `200`+valid→resolved, all else throws), supplier-classifies/consumer-maps across all five `lookup()` consumers (no wrapper), **no kill-switch** (the off-ramp would be the vuln), **zero Rego** (the fallback is retained); closes the one widening-on-failure path ([[PERMISSION-CATEGORIES-REVIEW]] C1/C4, aggravated by 6.5) | Accepted (planned) | abac · architecture · security |
| [0015](0015-control-plane-vocabulary-categorization.md) | Control-plane vocabulary categorization (Phase 6.7): extend the 6.5 coarse-category model to the `team:*` management verbs — one new category **`CONTROL`** → `[add-member, change-role, remove-member]` (the coarse `manage` verb split so it is deny-refinable), `list-members` added to `READ`, `team.rego` becomes category-driven via the shared `permissions.effective_actions` (symmetric with `catalog.rego`), `define-roles`/`transfer-ownership` stay **owner-only-by-code fences** outside the category system; `TeamRoleCapabilities` recast to emit category tokens; custom roles stay **management-incapable** (I12; `validateContract` now 422s the dead-data case); `TAG` left intact (`define-tags` re-gated mechanically — same outcomes); the two-axis principle (verb category vs the untouched `MembershipService` escalation gates); no DB migration; B2's tri-state supplier contract untouched | Accepted (planned) | abac · architecture · user-service |
| [0016](0016-action-enrichment-affordance-metadata.md) | Action enrichment / affordance metadata (Phase 6): an automatic `ResponseBodyAdvice` attaches an `_actions` map (bare-verb keys → `true`/`false`) to returned `Enrichable` resources/pages — read-side affordance, **not** enforcement; **inline on the DTO** via the OpenAPI `x-implements` marker + an explicit `readOnly` `_actions` property (wrapping `Authorized<T>` rejected); the **per-type sub-interface carries `abacResourceType()` + `abacActions()`** (it *is* the registry + validation allowlist — a separate SPI bean rejected); verdicts computed against **resolved attributes** from the generalized **`AbacResourceCache` (relocated to `opa-abac-core`)**, the list path write-through its post-filter survivors (re-resolve-in-advice rejected — double-load/drift); **reuses `allowAll` verbatim** (zero `OpaClient`/Rego change); the **omit-on-failure** degrade contract (present ⇒ complete real verdict, absent ⇒ couldn't-compute; all-false-on-failure rejected as a fail-open footgun); **affordance honesty** — enumerate only fully-OPA-decided verbs (team's Java-co-gated `change-role`/`define-roles`/`transfer-ownership` excluded); `opa.abac.action-enrichment.enabled` kill-switch; cache stays an attribute snapshot, never a verdict | Accepted (planned) | abac · architecture · spring |
| [0017](0017-cross-service-http-resilience.md) | Cross-service HTTP resilience (Slice B3): a uniform retry/backoff/circuit-break posture over all three cross-service HTTP edges (`HttpOpaClient`, `HttpRoleDefinitionSupplier`, `TagDefinitionClient`) that softens B2's outage→deny wall **without** re-opening the realm fallback — OPA resilience as a **decorator over the existing `OpaClient` interface** (no pluggable transport in core), the fail-closed contract **identical in every breaker/config state** (`compile`→`error()` with `fromError=true`, never `denyAll()`/`allowAll()` — the 5.5-B widening landmine), **side-effect-free** retry (incl. read-timeout; future mutating edges opt out), **asymmetric per-edge budgets** (OPA 1 retry / resolve+tag 2; no resilience under a DB lock), **three breakers** per-endpoint with **breaker outcome-invariance** (latency/load only, never a decision input), **optional/conditional Resilience4j** in the starter, a thin backend-agnostic **`CallGuard` seam** (R4j today, Boot-4 native later — second artifact line deferred), a **per-edge kill-switch** (the principled inverse of B2 — off = a safe baseline), **virtual-time tests** (injectable clock); zero core/Rego change | Accepted (planned) | abac · architecture · spring |
| [0018](0018-team-scoped-resource-isolation.md) | Team-scoped resource isolation (Slice B4): team membership is the **sole** access path to the catalog hierarchy — a role-def-only `filter` entrypoint + a `GovernedScopeResolver` SPI supplying the list's base `id IN (governed ids)` scope; the realm-role fallback **removed** from the single-decision path (a narrow `catalog:create` fallback retained — creation precedes membership); fail-closed throughout; core untouched | Accepted | abac · architecture · security |
| [0019](0019-pluggable-cross-service-ownership.md) | Pluggable cross-service resource-ownership resolution (Slice B4): a `ResourceOwnershipResolver` SPI + config-driven discovery client (TTL cache) reading a standard `/internal/{type}/{id}/created-by` contract, wired into `createTeam` to close target-squatting; fail-closed to `false` on every breach (unknown type / unreachable / 404 / mismatch) | Accepted | abac · architecture · security |
| [0020](0020-user-directory-port.md) | Pluggable user-directory port (Slice 2, user-directory work): a pure **search** read-model SPI `UserDirectory.search(query, limit) → List<DirectoryUser>` in `opa-abac-spring-security` (mirrors `ResourceOwnershipResolver`; provisioning out of scope), a concrete `KeycloakUserDirectory` in its **own optional module** auto-wired like B3's R4j (`@ConditionalOnClass`+`@ConditionalOnProperty`, `NoOp` fallback), least-privilege `client_credentials` + `view-users`-only service account, a bearer-only `search` endpoint under `/api/v1/users` returning a **bounded plain list** (disclosure type-bounded to `{subject,displayName}`), URL-agnostic port; **fail-closed to empty, no-oracle** (outage indistinguishable from genuine-empty — differ only in logs), `limit` clamped (default 20, max 50) | Accepted | security · architecture · api |
| [0021](0021-load-testing-methodology.md) | Load-testing methodology (Phase 7.2): a committed **k6** harness (host-run, `scripts/load/`, the postman-runner idiom) measuring four library hot paths through the real rig — the **gate-overhead delta** via a two-pass `ENABLE_OPA=0` rig flip (never a dedicated unguarded endpoint), **open-model** `constant-arrival-rate` (50 req/s default; separate discarded warm-up; 120 s windows; `REPS=3` official medians), an **operationally-defined ceiling** (ramping stages; knee = p99 > 1 s OR >1 % fails/drops sustained), **Jaeger-attributed amplification** with expected-vs-measured bounds, faults via the B3 stub + `docker pause` on OPA (three-phase timeline), fixtures bulk-seeded under the reserved `dddd…` prefix with the dedicated `perf` realm user; **report-only** numbers with **validity-only** gates into a root `PERFORMANCE.md`; no CI perf runs | Accepted (planned) | architecture · infra |
| [0022](0022-root-read-tag-exemption.md) | Root-read tag exemption (7.1): `view`/`list` on the **governing root** are exempt from a role's `required_tags` when `data.config.root_read_tag_exemption` is true (shipped default; **absent = strict, fail-closed**) — a role-level attribute filter must not void the explicit team binding on the resource the team targets; mutations + everything below the root stay tag-gated; catalogs become **taggable** (category-style delta dispatch, tag-on-create 422, no extra taggability knob); the 7.0.5 list↔GET **agreement** invariant holds in both flag states | Accepted | abac · rego · architecture |
| [0023](0023-request-scoped-resolution-memoization.md) | Request-scoped resolution memoization (Slice 7.3): memoizing decorators over the `RoleDefinitionSupplier` + `AncestorResolver` **beans** (BPP-wired, the B3 decorator idiom; role memo in spring-security, ancestor memo in the starter — the module-dependency constraint) storing **all three** tri-state outcomes per target — resolved / authoritative-empty / **the outage marker, re-thrown on replay** — pinning **"one request, one answer per target"**; staleness contract = a per-request snapshot (revocation at the next request boundary; nothing survives the request); pure pass-through outside a web request; one flag `opa.abac.resolve-memo.enabled` (default on) governs both memos, memoization only | Accepted (planned) | abac · architecture · spring |
| [0024](0024-batch-role-resolution.md) | Batch role resolution (Slice 7.3): `lookupAll(userId, Set<ResolveTarget>)` as an **additive default method** on `RoleDefinitionSupplier` (core, pure JDK, `@FunctionalInterface` preserved) — **two-state entries** (resolved / no-role), **whole-batch outage** (any transport/parse/status failure → `RoleResolutionException` for all targets; the server answers 5xx over a partial body), **strict completeness** (exactly one entry per requested target or the batch is malformed → outage); wire = one guarded `GET /internal/effective-roles` (repeated `target=<type>:<id>`, `200`-only, in-body `null` for no-role, never gateway-routed) riding the same `resolveCallGuard` as **one** call; the enrichment advice batches **unconditionally** (coalescing, not caching — not flag-gated) | Accepted (planned) | abac · architecture · spring · user-service |

| [0025](0025-taggable-products.md) | Taggable products (strict leaf semantics, filtered list): products adopt the category tag pattern end to end — `tags` on the wire contract, tag-on-create **accepted** (the governing team pre-exists, unlike a catalog's), the PUT delta-dispatched via `TagDecisionGate`, `assign-tags` in the enrichment set — and `product:list` moves off the plain page onto the **partial-eval data-filter cut** (`ProductListAuthorizer` + the `product.rego` `filter` entrypoint); ADR 0022's read exemption stays **root-only** (no leaf-read exemption); PUT full-replace tags semantics now cover products (clients echo tags or clear them) | Accepted | abac · security · architecture |
| [0026](0026-spring-boot-4-single-line-port.md) | Spring Boot 4 single-line port: 1.0 targets **Boot 4.0.x on Java 25 / Gradle 9.x** as a single-line artifact (the dual 3.5/4.0 door closed; the last 3.4 commit tagged `pre-sb4-port`); **Jackson 3 hard swap** in core with wire-format parity **asserted not assumed** (OPA `input` / JWT claims / jsonb tags each pinned; default-flips restored explicitly); covariant `authorize()` retires `check()`; **R4j 2.4.0** eliminates the `internal.CircuitBreakerStateMachine` coupling via the public `clock()` config seam (amends ADR 0017); byte-identical behavior is the port's acceptance frame | Accepted | architecture · spring |
| [0027](0027-maven-central-release-engineering.md) | Maven Central release engineering (the 1.0 blocker): **groupId `dev.dmitriikonovalov`** (DNS-TXT-verified, matches the package root); publish **all 5 library modules + a new `opa-abac-bom`** platform (examples never); **`com.vanniktech.maven.publish`** (Central-Portal-native, reads the `POM_*`/`GROUP`/`VERSION_NAME` props the repo already declares) — not the sunsetting OSSRH path; **local-only GPG signing** (`~/.gnupg` + `~/.gradle`, key never a GitHub secret) + a **manual** first release; broad `.gitignore` keystore hardening before any key exists; version **`1.0.0`** then bump `main` → `1.1.0-SNAPSHOT`; javadoc jar with `-Xdoclint:none`; **build/release infra only — no library source, no `opa-abac-core` Spring dependency** | Accepted | architecture · build |
| [0028](0028-agent-tool-call-authorization.md) | Agent tool-call authorization (Phase 9): a **two-layer** decision model — a **new tool-gate** Rego document evaluated in an `example-mcp-server` (subject + statically declared tool action/category/risk-tags, explicit `default allow := false`, also powering the `allowAll` roster batch) over the **untouched** per-type catalog policies as the target-gate; **enforcement by composition** — the principal-ceiling ∩ agent-capability intersection is enforced *across* the layers, the MCP server forwards the caller's token unmodified and **never asserts a role** downstream (the caller-supplied-authority shape ADR 0018 removed), so it can only fail to narrow, never widen; an **additive** dual-identity subject (`actor` + `chain`, RFC 8693 `act` semantics, normalized by a `DelegationChainExtractor` from a stock-Keycloak custom claim; **no actor = an ordinary human call**); agent capability **narrows only** (`AgentCapabilitySupplier`, tri-state, turn-scoped memo, intersection computed in Rego — no standing agent grants, an agent is never a principal); **example-first** packaging with library extraction as the slice's exit criterion; fail-closed edges + kill-switches whose OFF state is never wider than ON; honest cost: the per-type policies do not see the actor this slice | Accepted | abac · opa · spring |
| [0029](0029-supervised-read-scope.md) | Supervised read scope (SUPERVISOR-STEP-UP): a **second, disjoint** access path beside membership — a **derived id set** re-derived per request (subject → transitive reports → their **CONTROL-capable** teams → catalogs), depth-capped at 10 and cycle-guarded, consumed through a **new fail-closed org-relation seam** (`transitiveReportsOf`, never null/throwing, empty on breach) that is **example-side** this slice and rides neither the frozen `UserDirectory` port nor a Keycloak-native relation; **membership always wins** so `supervised := S \ M` — disjointness is load-bearing, making the mixed-subject fail-open *structurally impossible* rather than a check that must be written right; `/internal/effective-role` gains a **non-membership branch** returning a **synthesized read-only** role (the realm claim is a **UX-only eligibility marker, never resolver input** — claim + zero reports = sees nothing), whose reserved code carries **provenance** with zero envelope change (spoofing it is self-demotion, not escalation); the root list becomes a **two-leg partitioned query**, retiring `governedIds.get(0)`; errored source → own memberships, partial derivation → membership-only, never a partial supervised set | Accepted (planned) — **amended by [0031](0031-inheritance-confined-to-membership-roles.md)** | abac · spring |
| [0030](0030-step-up-decision-contract.md) | Step-up authentication contract (SUPERVISOR-STEP-UP): **no new verb** — the root GET is already metadata-only, so the sensitive act is opening **child contents** under an `env=production` root, and elevation attaches to the **supervised path only** (ordinary members unaffected); `env` is protected by a **new additive `operatorManaged`** flag carried through `TagDefinitionView` and enforced in `TagAssignmentService` (the `abac_deny` precedent does *not* transfer — it is protected by *not existing* — and `is_system` already means definition-immutability on keys the demo assigns), with **no runtime set path** so untagged=non-prod is safe; the root's tag reaches child decisions as **`root_attributes`** fetched by the *path's* catalogId (leaving `ParentRef` and both ancestor resolvers untouched); **RS-side freshness is the whole control** (`loa ≥ 2 ∧ now − auth_time ≤ max_age + skew`) — **measured** on the rig: refresh preserves `auth_time`/`acr` while `iat` advances, so the dual-token/short-TTL apparatus is **dropped**; the step-up deny travels as an **additive, omitted-when-absent `deny_reason`** (no version bump), mapped to an **RFC 9470 401** *only* for that deny while plain denials stay 403, and **the client must forward `max_age`** or it loops forever; audit ships the **emission point** only | Accepted (planned) | abac · opa · spring |
| [0031](0031-inheritance-confined-to-membership-roles.md) | Ancestor inheritance is confined to **membership-derived** roles: `attributes.provenance` (one key, a value vocabulary) is stamped at the single resolution funnel `resourceRole()`, and `inherited_grant`/`list_inheritable_grant` in `category.rego`+`product.rego` open only on `== "membership"` — so every **synthesized** role (supervisor now, tiered/elevated later) is confined to the types it names explicitly, **fail-closed by default**; direct grants and stored rows are untouched. **Amends [0029](0029-supervised-read-scope.md) row 131** — "contents close themselves, zero Rego changes" was disproven by `opa eval` (the pinned `catalog:[READ]` role inherited `category:view`/`product:view` whenever ancestors were present, which is always at runtime; the ancestor-less acceptance probe was a green-lighting trap) | Accepted (planned) | abac · rego |
| [0032](0032-root-attribute-enrichment-input-contract.md) | Root-attribute enrichment input contract (PRODUCTION-TIER): `AbacContext.Resource` gains an **additive** fifth component `root_attributes` (the `ancestors` evolution pattern — compat constructors, absent = byte-identical wire) carrying the governing root's **full tag map**; **three distinguishable states** — absent = enrichment failed ⇒ tier **unproven** ⇒ the supervised path closes · `{}` = untagged ⇒ non-production (ADR 0030 §3) · tagged = as tagged — with **`NON_NULL`** serialization, because `NON_EMPTY` would merge untagged with fetch-failed, the exact fail-open the contract exists to prevent; population is **manager-side** — the authorization manager resolves the governing root through the app's existing `AbacResourceResolver` SPI, read-through-memoized in the request cache (no new `AbacResource` method; the library orchestrates, the app's resolver fetches); the naive `not root_attributes.env == "production"` policy shape is **wrong** (an absent `env` passes a negated comparison) — the provenance-scoped two-clause `denied` pair is the reference shape | Accepted (planned) | abac · opa · spring |
| [0033](0033-catalog-provenance-affordance.md) | The `_provenance` affordance on catalog rows (SPA-CHALLENGE-UX): `Catalog` list items + the single GET gain an additive, optional, read-only `_provenance: "member" \| "supervised"` — **an affordance, not enforcement** (the ADR 0016 stance) so a client can label supervised rows and *predict* "verify to open" without marking a member's production catalog; **one semantic, two derivations pinned to agree** (list = the two-leg id set; GET = the resolved role's ADR 0031 provenance stamp — equal by construction, IT-pinned); **absent when not computed, never `null`/default** (a failed GET lookup, a request that bypassed the authorizer), while every degrade branch that can still label honestly does; mechanism = the `_actions` one **example-side** (a second `x-implements` marker with `@JsonInclude(NON_NULL)`, an in-place `ResponseBodyAdvice`, the id set as a request attribute; `type: string` + doc `enum`, not an inline enum class); zero library change; the GET's memo-off cost documented | Accepted (planned) | abac · catalog-service · api |

## Related
- The example app these decisions shape: [[USER-MANAGEMENT-SERVICE]] (Phase 4) and [[TAG-DICTIONARY]] (Phase 4.5).
- The library slices they feed/pin: [[DATA-FILTERING]] (Phase 5) · [[ACTION-ENRICHMENT]] (Phase 6).
- The authorization model they feed: [[TEAM-BASED-AUTHORIZATION]], [[TAG-BASED-AUTHORIZATION]], [[ABAC-AUTHORIZATION]].
- The library base they build on: [[DOMAIN-MODEL]].
