# Prompts — copy-paste starting points for readers with an AI assistant

| File | Use it when |
|---|---|
| [[RUN-THE-DEMO]] | you have a coding agent on your machine and want it to run the rig and walk the console for you, reporting the cut it observed |
| [[FROM-EXAMPLE-TO-YOUR-APP]] | you want to turn the example into your own application — the shape, the rename surface, the order of work, and a prompt per step |

They are written for any agent that can run shell commands (the repo is built with Claude Code, but
nothing here depends on it). Both assume you have read the [[DEMO-CONSOLE-WALKTHROUGH]] once.
